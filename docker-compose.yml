version: "3.9"
services:
  app:
    restart: unless-stopped
    build:
      dockerfile: Dockerfile
      context: .
      target: dev
    volumes:
      - ./downloads:/usr/src/app/downloads
      - ./downloads.log:/usr/src/app/downloads.log
    ports:
      - "17945:17945"
